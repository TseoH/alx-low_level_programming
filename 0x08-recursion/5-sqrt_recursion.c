#include "main.h"

/**
*_pow_recursion - printf _pow_recursion
*
*Description: _pow_recursion print
*@n: Integer
*Return: Integer
*/
int _sqrt_recursion(int n)
{
	int i = n;
	int x = i / 2;
	if ((x * x) == n)
	{
		return x;
	}
}
