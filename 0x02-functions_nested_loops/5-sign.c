#include "main.h"

/**
* print_sign - print print_sign
*
* Description : print_sign printer
* @n: param
* Return: 1 if True
* Or 0 if False
*/
int print_sign(int n)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
		return (1);
	else
		return (0);
}
